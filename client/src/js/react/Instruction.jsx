/** @jsx React.DOM */
var React = require('react');

var Instruction = React.createClass({
  getInitialState: function () {
    return {};
  },

  render: function () {
    return <div className='instruction screen' />;
  }
});

module.exports = Instruction;
